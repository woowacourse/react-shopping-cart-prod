import ValidateText from 'components/Common/ValidateText/ValidateText';

export default {
  title: 'components/ValidateText',
  component: ValidateText,
};

const Template = (args) => <ValidateText {...args} />;

export const Example = Template.bind({});
