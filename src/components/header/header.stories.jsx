import Header from "@/components/header/Header";

export default {
  component: Header,
  title: "Header",
};

const Template = () => <Header />;

export const Defaults = Template.bind({});
Defaults.args = {};
