import Dropdown from "@/components/Dropdown";

export default {
  component: Dropdown,
  title: "Dropdown",
};

const Template = () => <Dropdown />;

export const Defaults = Template.bind({});
Defaults.args = {};
