import Dropdown from "@/components/Dropdown";

export default {
  component: Dropdown,
  title: "Dropdown",
};

function Template() {
  return <Dropdown />;
}

export const Defaults = Template.bind({});
Defaults.args = {};
