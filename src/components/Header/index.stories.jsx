import Header from "@/components/Header";

export default {
  component: Header,
  title: "Header",
};

function Template() {
  return <Header />;
}

export const Defaults = Template.bind({});
Defaults.args = {};
