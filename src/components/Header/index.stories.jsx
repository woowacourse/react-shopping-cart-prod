import Header from "@/components/Header";

export default {
  component: Header,
  title: "Header",
};

const Template = () => <Header />;

export const Defaults = Template.bind({});
Defaults.args = {};
