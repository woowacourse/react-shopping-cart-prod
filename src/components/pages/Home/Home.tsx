import { Layout } from '../../common/Layout/Layout';
import { ProductList } from '../../ProductList/ProductList';

export const Home = () => {
  return (
    <Layout>
      <ProductList />
    </Layout>
  );
};
