import styled from 'styled-components';
import { LoginBox, LoginButton, LoginInput, LoginLabel, LoginWrapper } from './Login.styles';

export const SignUpBox = styled(LoginBox)``;

export const SignUpWrapper = styled(LoginWrapper)``;

export const SignUpLabel = styled(LoginLabel)``;

export const SignUpInput = styled(LoginInput)``;

export const SignUpButton = styled(LoginButton)``;
