import PasswordEditModal from '.';

export default {
  title: 'Components/PasswordEditModal',
  component: PasswordEditModal,
};

const Template = args => <PasswordEditModal {...args} />;

export const PasswordEditModalTemplate = Template.bind({});
PasswordEditModalTemplate.args = {};
